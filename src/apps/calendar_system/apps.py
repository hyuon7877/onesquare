from django.apps import AppConfig


class CalendarSystemConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.calendar_system'
    verbose_name = 'Calendar System'